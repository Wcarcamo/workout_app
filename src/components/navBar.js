import React from 'react';
import ReactDOM from 'react-dom';

class Bar extends React.Component {
  render() {
    return (
      <div>
        <button>Log</button>
        <button>Track</button>
      </div>
    );
  }
}
  
export default Bar;